name: OWASP Dependency-Check

on:
  push:
    branches:
      - main

jobs:
  owasp-dependency-check:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code

    - name: Set up Node.js
      with:
        node-version: '16'  # Specify the Node.js version you want to use

    - name: Set up Java
      uses: actions/setup-java@v2
      with:
        java-version: '11'  # Specify the Java version you want to use
        distribution: 'adopt'  # Specify the distribution (e.g., 'adopt', 'adopt-lts', 'corretto', 'zulu', etc.)

    - name: Run OWASP Dependency-Check
      run: |
        curl -LO https://github.com/jeremylong/DependencyCheck/releases/download/v8.4.2/dependency-check-8.4.2-release.zip
        unzip dependency-check-8.4.2-release.zip
        chmod +x dependency-check/bin/dependency-check.sh
        ./dependency-check/bin/dependency-check.sh --project "Dependency Check Example" --scan /vcs-hosting/ --out /tmp --format ALL
      env:
        MAVEN_OPTS: -Dmaven.repo.local=${{ runner.workspace }}/.m2/repository
        MVNW_HOME: ./mvnw

    - name: Upload Dependency-Check Report
      uses: actions/upload-artifact@v2
      with:
        name: dependency-check-report
        path: /tmp/dependency-check-report.json
